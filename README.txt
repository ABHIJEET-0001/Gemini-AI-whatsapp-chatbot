# Gemini WhatsApp Chatbot

## Setup Instructions

1. Install Python packages:

    pip install -r requirements.txt

2. Add your API keys in `.env` file

3. Run the Flask app:

    python app.py

4. Visit `http://localhost:5000` to test web chatbot

5. Connect Twilio Sandbox to webhook `/message` for WhatsApp
